[package]
name = "zectrix-extended"
version = "0.1.0"
description = "A Tauri App"
authors = ["you"]
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lib]
# The `_lib` suffix may seem redundant but it is necessary
# to make the lib name unique and wouldn't conflict with the bin name.
# This seems to be only an issue on Windows, see https://github.com/rust-lang/cargo/issues/8519
name = "zectrix_extended_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-opener = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

# ===== Zectrix Note4 云平台 API 集成 =====
# 注意：reqwest 0.13 起 `RequestBuilder::query()` 被挪到了可选的 "query" feature 后面，
# 不开启该 feature 会报 "no method named `query` found"。json / multipart 同理。
reqwest = { version = "0.13", features = ["json", "multipart", "query"] }
thiserror = "2"
keyring = "4"
base64 = "0.22"


# Read the optimization guideline for more details: https://tauri.app/concept/size/#cargo-configuration
[profile.release]
codegen-units = 1
lto = true
opt-level = 3
panic = "abort"
strip = true
